name: Keep Supabase Active

on:
  schedule:
    # A cada 3 dias as 9h UTC (folga confortavel antes do limite de 7 dias)
    - cron: '0 9 */3 * *'
  workflow_dispatch:

jobs:
  ping-supabase:
    runs-on: ubuntu-latest
    steps:
      - name: Ping REST/PostgREST (atividade que CONTA p/ o auto-pause)
        # O auth/v1/health NAO reseta o contador de inatividade do Supabase
        # (projeto pausava mesmo com a Action verde). O que conta e trafego
        # real na Data API: GET numa tabela com leitura publica via anon key.
        run: |
          set -e
          ANON="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InF5aWt1YnVxeWhvYnBwdm9qdnBhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzM5ODM5NDUsImV4cCI6MjA4OTU1OTk0NX0.YYhbW3KrkXtBDBb4Wpnvfrbl8hzb8-ixet54prpD6_U"
          RES=$(curl -s -w "\n%{http_code}" \
            -H "apikey: $ANON" \
            -H "Authorization: Bearer $ANON" \
            "https://qyikubuqyhobppvojvpa.supabase.co/rest/v1/foods?select=id&limit=1")
          CODE=$(echo "$RES" | tail -1)
          echo "NutriTrack REST ping: HTTP $CODE"
          [ "$CODE" = "200" ] || (echo "ERRO" && exit 1)

      - name: Executar SQL real no Postgres (camada extra)
        run: |
          set -e
          RES=$(curl -s -w "\n%{http_code}" -X POST \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_PAT }}" \
            -H "Content-Type: application/json" \
            -d '{"query":"SELECT 1 as alive, now() as ts"}' \
            "https://api.supabase.com/v1/projects/qyikubuqyhobppvojvpa/database/query")
          CODE=$(echo "$RES" | tail -1)
          BODY=$(echo "$RES" | sed '$d')
          echo "NutriTrack SQL ping: HTTP $CODE | $BODY"
          [ "$CODE" = "200" ] || [ "$CODE" = "201" ] || (echo "ERRO" && exit 1)
